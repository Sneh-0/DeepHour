# Full URL of the deployed API, including the /api suffix.
# Leave UNSET for local dev — Vite proxies /api to localhost:4000 instead.
# Production example:
VITE_API_URL=https://your-api-host.onrender.com/api
